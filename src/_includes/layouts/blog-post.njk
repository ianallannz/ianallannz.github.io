<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="stylesheet" href="/css/custom.css">
</head>

<body>
    {% include "lines.njk" %}

    <header>
        {% include "navbar.njk" %}
        <div class="page-name blog-post-name">
            <h1># <a href="/blog">Blog</a> post</h1>
        </div>
        {% include "profile.njk" %}
    </header>
    <main>
        <section id="blog-post" class="blog-post">
            <article>
                {{ content | safe }}
            </article>

            {% set relatedPosts = collections.relatedPostMap.get(page.url) %}
            {% if relatedPosts %}
            <div class="related-posts">
                <h2>## Related Posts</h2>
                <ul>
                    {% for post in relatedPosts %}
                    <li><a href="{{ post.url }}">{{ post.data.title }}</a></li>
                    {% endfor %}
                </ul>
            </div>
            {% endif %}


        </section>


    </main>
    {% include "modal-contact.njk" %}
    {% include "footer.njk" %}

    <script src="/js/custom.js" defer></script>
</body>

</html>